
namespace MyApiProject.Enums
{
    public enum BusinessStatus { Pending, Approved, Denied }

    public enum AccountType { RegularUser, Business, Admin, CityAdmin, SubManager }

    public enum messageStatus
    {
        Replied, Viewed, New
    }
}
